<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/light_yellow</item>
        <item name="colorPrimaryDark">@color/light_yellow</item>
        <!-- No Title Bar-->
        <item name="windowNoTitle">true</item>
        <!-- No Status Bar (Full Screen)-->
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Theme.Introduce_Me -->
    <style name="Theme.Introduce_Me" parent="Base.Theme.Introduce_Me">
        <!-- 버튼 스타일 지정 -->
        <item name="android:buttonStyle">@style/WhiteTextButton</item>
    </style>

    <!-- WhiteTextButton -->
    <style name="WhiteTextButton" parent="Widget.AppCompat.Button">
        <!-- 버튼 글씨 색상 설정 (흰색) -->
        <item name="android:textColor">@color/white</item>
    </style>
</resources>